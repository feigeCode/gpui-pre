[package]
name = "gpui-pre-http-client-tls"
version = "0.3.99"
edition = { workspace = true }
publish = true
license = "Apache-2.0"
description = "Zed's `http_client_tls` crate (gpui-pre snapshot of zed@7ea59c3)"
repository = "https://github.com/zed-industries/zed"

[package.metadata.gpui-pre]
zed-crate = "http_client_tls"
zed-version = "0.1.0"
zed-rev = "7ea59c309482ad870d3d4bedd27492833e6f497f"

[lib]
path = "src/http_client_tls.rs"
doctest = true
name = "http_client_tls"

[features]
test-support = []

[dependencies]
log = { workspace = true }
rustls = { workspace = true }
rustls-platform-verifier = { workspace = true }
webpki-roots = { workspace = true }

[lints]
workspace = true
