[package]
name = "gpui-pre-util"
version = "0.3.99"
publish = true
edition = { workspace = true }
license = "Apache-2.0"
description = "Zed's `gpui_util` crate (gpui-pre snapshot of zed@840b0e9)"
repository = "https://github.com/zed-industries/zed"

[package.metadata.gpui-pre]
zed-crate = "gpui_util"
zed-version = "0.1.0"
zed-rev = "840b0e9b5fd8a3932a7e0f3afef9696ef0ec40e3"

[lib]
name = "gpui_util"

[features]

[dependencies]
log = { workspace = true }
anyhow = { workspace = true }

[target.'cfg(target_os = "windows")'.dependencies]
which = { workspace = true }

[lints]
workspace = true
