[package]
name = "gpui-pre-ztracing-macro"
version = "0.3.99"
edition = { workspace = true }
publish = true
license = "Apache-2.0"
description = "Zed's `ztracing_macro` crate (gpui-pre snapshot of zed@ed16e53)"
repository = "https://github.com/zed-industries/zed"

[package.metadata.gpui-pre]
zed-crate = "ztracing_macro"
zed-version = "0.1.0"
zed-rev = "ed16e536e7988825a911fac00d013523ef67ef25"

[lib]
proc-macro = true
test = false
doctest = false
name = "ztracing_macro"

[features]
