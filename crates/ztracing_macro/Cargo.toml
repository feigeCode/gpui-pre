[package]
name = "gpui-pre-ztracing-macro"
version = "0.3.99"
edition = { workspace = true }
publish = true
license = "Apache-2.0"
description = "Zed's `ztracing_macro` crate (gpui-pre snapshot of zed@aeaf3b4)"
repository = "https://github.com/zed-industries/zed"

[package.metadata.gpui-pre]
zed-crate = "ztracing_macro"
zed-version = "0.1.0"
zed-rev = "aeaf3b4f20a23cf002f678ef925e6a9a02f00358"

[lib]
proc-macro = true
name = "ztracing_macro"

[features]

[dependencies]
