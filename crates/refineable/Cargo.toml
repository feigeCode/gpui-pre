[package]
name = "gpui-pre-refineable"
version = "0.3.99"
edition = { workspace = true }
publish = true
license = "Apache-2.0"
description = "A macro for creating 'refinement' types that can be used to partially initialize or mutate a complex struct (gpui-pre snapshot of zed@beeef1b)"
repository = "https://github.com/zed-industries/zed"

[package.metadata.gpui-pre]
zed-crate = "refineable"
zed-version = "0.1.0"
zed-rev = "beeef1b22a8d4a236e308af232067b4503f69fcd"

[lib]
path = "src/refineable.rs"
doctest = false
name = "refineable"

[features]

[dependencies]
derive_refineable = { workspace = true }

[lints]
workspace = true
