[package]
name = "gpui-pre-refineable"
version = "0.3.99"
edition = { workspace = true }
publish = true
license = "Apache-2.0"
description = "A macro for creating 'refinement' types that can be used to partially initialize or mutate a complex struct (gpui-pre snapshot of zed@fe3e62a)"
repository = "https://github.com/zed-industries/zed"

[package.metadata.gpui-pre]
zed-crate = "refineable"
zed-version = "0.1.0"
zed-rev = "fe3e62a81ad4c823650cc71efac3143fde76e2aa"

[lib]
path = "src/refineable.rs"
doctest = false
test = false
name = "refineable"

[features]

[dependencies]
derive_refineable = { workspace = true }

[lints]
workspace = true
