[package]
name = "gpui-pre-refineable"
version = "0.3.99"
edition = { workspace = true }
publish = true
license = "Apache-2.0"
description = "A macro for creating 'refinement' types that can be used to partially initialize or mutate a complex struct (gpui-pre snapshot of zed@ad01cf9)"
repository = "https://github.com/zed-industries/zed"

[package.metadata.gpui-pre]
zed-crate = "refineable"
zed-version = "0.1.0"
zed-rev = "ad01cf9995803606272bb58f2abe2d27887d87e0"

[lib]
path = "src/refineable.rs"
doctest = false
test = false
name = "refineable"

[features]

[dependencies]
derive_refineable = { workspace = true }

[lints]
workspace = true
